# Copy this file to .env.local. Both variables are needed at build time.
# The values are not secret — the dataset is public and the site reads only
# published content. Cloudflare needs the same two as build variables.
NEXT_PUBLIC_SANITY_PROJECT_ID=8d65o0bs
NEXT_PUBLIC_SANITY_DATASET=production
